
<%= form_for(@product) do |f| %>

  <%= f.label(:name) %>
  <%= f.text_field(:name) %>

  <%= f.label(:description) %>
  <%= f.text_area(:description) %>

  <%= f.label(:website) %>
  <%= f.text_field(:website) %>

  <%= f.submit 'Submit' %>

<% end %>
