<%= render 'shared/error_messages', object: @product %>
<%= render 'shared/error_messages', object: @comment %>

<%= simple_form_for [@product, @comment] do |f| %>
  <%= f.input :description, required: true, autofocus: true %>
  <%= f.button :submit, "Review Item" %>
<% end %>
